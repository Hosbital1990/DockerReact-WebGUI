
version: "3.9"

services:
  frontend:
    build:
      context: .
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/frontend  # Sync frontend folder
      - /frontend/node_modules # Prevents overwriting node_modules


